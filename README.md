# Pet Project - CV card of developer:

To run app in developer mode use `npm install`
after run app with `npm start` in terminal

# App using:

`React`
`Tailwind`
`ThreeJS`
`Eslint`
`Prettier`
`EmailJS`

# Created by Kalashnik Yury
